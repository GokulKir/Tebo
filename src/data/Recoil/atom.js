import { atom } from "recoil"


export const Backid = atom({
    key : "Backid",
    default : null
})

export const Visible = atom({
    key : "Visible",
    default : false
})

export const SsidValue = atom({
    key : "SsidValue",
    default : null

})

export const SSidconnectionEstablished = atom({
    key : "ConnectionEstablished",
    default : null
})

export const WifiConnectionEstablished = atom({
    key : "WifiConnectionEstablished",
    default : null
})

